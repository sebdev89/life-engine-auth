# Activate test profile for all core-app tests (no shell exports required).
spring.profiles.active=test
# Set before ApplicationEnvironmentPreparedEvent so the env listener sees a non-blank value (WebMvcTest slices
# may not have resolved @ActiveProfiles yet when that event runs).
lifeengine.deployment.env=test
